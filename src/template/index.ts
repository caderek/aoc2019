import u from "../utils"

const rawInput = u.readInput()
const input = rawInput

const goA = (input) => {
  return
}

const goB = (input) => {
  return
}

// u.test(goA(), )
console.log("A:", goA(input))

// u.test(goB(), )
console.log("B:", goB(input))
